## Tasks:

**1. What you understood in Jenkin, write a small article in your own words (Don't copy from Internet Directly)**
A)
Jenkins is a tool used for automation of application by building, testing and deploying it. it performs continous integration and continous deployment. It uses java to perform the tasks in a server.
We can use plugins to configure those tools like git, maven etc.
We create jobs and run them on agents. The complete flow is called as a pipeline


**2.Create a freestyle pipeline to print "Hello World!!**
A) go to new item >> helloworldPipeLine >> freestyle project >> build steps >> shell >> echo "hello world" >> build now >> console output 