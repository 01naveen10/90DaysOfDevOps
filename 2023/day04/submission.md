**Tasks**

- Explain in your own words and examples, what is Shell Scripting for DevOps.
A) shell scripting is the way of accessign operating system using command line interface by writign scripts . It can make running the commands easy like creating or running servers and monitoring disk space etc instead of writing  commands repeatedly each and every time

- What is `#!/bin/bash?` can we write `#!/bin/sh` as well?
A) it is shebag and the first line in shell script which notifies system it is a shell script and executes the commands

- Write a Shell Script which prints `I will complete #90DaysOofDevOps challenge`
A) create script.sh >> #!/bin/bash then echo the line >>save >> bash script.sh

- Write a Shell Script to take user input, input from arguments and print the variables.
A) read name >> echo ${name}

- Write an Example of If else in Shell Scripting by comparing 2 numbers
A) if [ $a -gt $b ] then echo else fi
