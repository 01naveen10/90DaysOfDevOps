Let’s make a well articulated and documented **"cheat-sheet"** with all the commands you learned so far in Linux, Git-GitHub and brief info about its usage.

A)done


Linux: ls, cd, mkdir, rm, bash, ps, df, sudo, apt-get, touch, cat, vim, clear, man, echo, pwd, chmod, grep, awk, cut, curl, wget, ping


Git : git init, commit -m, add, push, pull, stash, stash pop ,stash list, git branch, checkout, cherry-pick, merge, rebase